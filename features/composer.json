{
    "type": "project",

    "scripts": {
        "tests": "behat --colors"
    },

    "autoload": {
        "psr-4": {
            "Features\\": "src/"
        }
    },

    "require": {
        "php": "^7.4",
        "ext-pdo": "*",
        "behat/behat": "^3.5",
        "guzzlehttp/guzzle": "^6.4",
        "phpunit/phpunit": "^9.0.0",
        "psr/container": "^1.0"
    },

    "config": {
        "sort-packages": true
    }
}
